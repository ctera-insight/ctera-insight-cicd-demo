# Merged values for tenant: customer1-qa
# Generated by CI - DO NOT EDIT MANUALLY
global:
  clusterName: cluster-01
  security:
    enabled: true
    networkPolicy: denyAll
  quotas:
    cpu: "10"
    memory: "20Gi"
  labels:
    managed-by: argocd
    platform: ctera-insight
  replicas: 2
  debug: false
  logLevel: info
  namespace: ns-customer1-qa

services:
  ui:
    enabled: true
    image:
      repository: ghcr.io/ctera-insight/ui-service
      tag: "0.1.0"
    resources:
      requests:
        cpu: "200m"
        memory: "256Mi"
      limits:
        cpu: "1000m"
        memory: "1Gi"
    ingress:
      enabled: true
      className: nginx
      domain: customer1.qa.local
    microservice:
      enabled: true
      env:
        LOG_LEVEL: info
        DEBUG_MODE: "false"

  billing:
    enabled: true
    image:
      repository: ghcr.io/ctera-insight/billing-service
      tag: "0.1.0"
    resources:
      requests:
        cpu: "200m"
        memory: "256Mi"
      limits:
        cpu: "1000m"
        memory: "1Gi"
    pipeline:
      flink:
        enabled: true
        parallelism: 2
        checkpointInterval: 60000
