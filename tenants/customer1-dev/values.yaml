# Merged values for tenant: customer1-dev
# Generated by CI - DO NOT EDIT MANUALLY
global:
  clusterName: cluster-01
  security:
    enabled: true
    networkPolicy: denyAll
  quotas:
    cpu: "10"
    memory: "20Gi"
  labels:
    managed-by: argocd
    platform: ctera-insight
  replicas: 1
  debug: true
  logLevel: debug
  namespace: ns-customer1-dev

services:
  ui:
    enabled: true
    image:
      repository: ghcr.io/ctera-insight/ui-service
      tag: "0.1.0"
    resources:
      requests:
        cpu: "100m"
        memory: "128Mi"
      limits:
        cpu: "500m"
        memory: "512Mi"
    ingress:
      enabled: true
      className: nginx
      domain: customer1.dev.local
    microservice:
      enabled: true
      env:
        LOG_LEVEL: debug
        DEBUG_MODE: "true"

  billing:
    enabled: true
    image:
      repository: ghcr.io/ctera-insight/billing-service
      tag: "0.1.0"
    resources:
      requests:
        cpu: "100m"
        memory: "128Mi"
      limits:
        cpu: "500m"
        memory: "512Mi"
    pipeline:
      flink:
        enabled: false
        parallelism: 1
        checkpointInterval: 60000
