# Merged values for tenant: customer1-staging
# Generated by CI - DO NOT EDIT MANUALLY
global:
  clusterName: cluster-01
  security:
    enabled: true
    networkPolicy: denyAll
  quotas:
    cpu: "10"
    memory: "20Gi"
  labels:
    managed-by: argocd
    platform: ctera-insight
  replicas: 2
  debug: false
  logLevel: warn
  namespace: ns-customer1-staging

services:
  ui:
    enabled: true
    image:
      repository: ghcr.io/ctera-insight/ui-service
      tag: "0.1.0"
    resources:
      requests:
        cpu: "500m"
        memory: "512Mi"
      limits:
        cpu: "2000m"
        memory: "2Gi"
    ingress:
      enabled: true
      className: nginx
      domain: customer1.staging.local
    microservice:
      enabled: true
      env:
        LOG_LEVEL: warn
        DEBUG_MODE: "false"

  billing:
    enabled: true
    image:
      repository: ghcr.io/ctera-insight/billing-service
      tag: "0.1.0"
    resources:
      requests:
        cpu: "500m"
        memory: "512Mi"
      limits:
        cpu: "2000m"
        memory: "2Gi"
    pipeline:
      flink:
        enabled: true
        parallelism: 4
        checkpointInterval: 30000
